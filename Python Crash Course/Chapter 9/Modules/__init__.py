# In Python projects, if you create a file called __init__.py
# in a directory then Python will treat that directory as a
# package. A package in Python is a collection of modules
# (individual .py files) that can be imported into other
# Python files.